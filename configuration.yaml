homeassistant:
  # Name of the location where Home Assistant is running
  name: Home
  # Location required to calculate the time the sun rises and sets
  latitude: 45.512794
  longitude: -122.679565
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: 15.24
  # metric for Metric, imperial for Imperial
  unit_system: imperial
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: America/Los_Angeles
  # Customization file
  customize: !include customize.yaml

weather:
  - platform: darksky
    api_key: !secret darksky_api_key
    mode: daily

ecobee:
  api_key: !secret ecobee_api_key

ring:
  username: !secret ring_username
  password: !secret ring_password

apple_tv:
  - host: !secret apple_tv_host
    login_id: !secret apple_tv_login_id

remote:
  - platform: harmony
    name: Living Room Harmony
    host: !secret harmony_hub_host

lovelace:
  mode: yaml

camera:
  - platform: ring

ios:

wemo:

# Show links to resources in log and frontend
# introduction:

# Enables the frontend
frontend:
  themes: !include_dir_named themes/

# Enables configuration UI
config:

system_health:

# Uncomment this if you are using SSL/TLS, running in Docker container, etc.
# http:
#   base_url: example.duckdns.org:8123

# Checks for available updates
# Note: This component will send some information about your system to
# the developers to assist with development of Home Assistant.
# For more information, please see:
# https://home-assistant.io/blog/2016/10/25/explaining-the-updater/
updater:
  # Optional, allows Home Assistant developers to focus on popular components.
  # include_used_components: true

# Discover some devices automatically
discovery:

# Allows you to issue voice commands from the frontend in enabled browsers
conversation:

# Enables support for tracking state changes over time
history:

# View all events in a logbook
logbook:

# Enables a map showing the location of tracked devices
map:

# Enable feedreeder
feedreader:
  urls:
    # New liquor licenses in Portland, OR
    - https://www.portlandoregon.gov/rss.cfm?c=61884
  scan_interval:
    minutes: 30

# Track the sun
sun:

# Binary sensors
binary_sensor: !include binary_sensors.yaml

# Sensors
sensor: !include sensors.yaml
  # Weather prediction
  # - platform: yr

# Text to speech
tts:
  - platform: google

# Cloud
cloud:

# media_player: !include devices/media_players.yaml
zone:
  - name: Home
    latitude: 45.492667610857865
    longitude: -122.63747532294663
    radius: 100
    icon: mdi:shield-home
  - name: School
    latitude: 45.49013526190419
    longitude: -122.63590812433561
    radius: 100
    icon: mdi:school

# 45.4896677,-122.638002
group: !include groups.yaml
automation: !include_dir_merge_list automations
script: !include scripts.yaml

http:
 # api_password: qZ@9Puvhk3}Wx
 ip_ban_enabled: true
 login_attempts_threshold: 3

panel_iframe:
 configurator:
   title: Configurator
   url: https://justinshome.duckdns.org:3218
   icon: mdi:xml
