sensor.cool_breeze_temperature:
  templates:
    icon: >-
       {% if is_state('binary_sensor.cool_breeze_occupancy', 'on') %}
        mdi:run-fast
       {% else %}
        mdi:run
      {% endif %}
